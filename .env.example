# ============================================================
# Travel Story — 地点搜索密钥
#
# 不配置时搜索会提示「未配置」，界面给不出结果。
# 两个 key 互补：
#   - 高德  → 国内 POI（精度高、数据新）
#   - LocationIQ → 国外（OSM 数据 + 中文名 + 知名地标排名）
# 至少配一个就能用；配齐两个效果最好。
#
# 复制本文件为 .env.local 再填值；.env.local 已在 .gitignore 中，不会提交。
# ============================================================

# 高德开放平台「Web服务」类型 key（国内主力）
# 获取：https://lbs.amap.com/ → 控制台 → 应用管理 → 创建应用
#       → 添加 key，服务平台选「Web服务」（个人开发者免费）
# 注意：必须是「Web服务」类型，「JS API」类型的 key 调不动文本搜索接口。
GAODE_KEY=

# LocationIQ key（国外主力；Nominatim 托管版，OSM 全球数据）
# 获取：https://locationiq.com/ → Sign up → Free 档（约 1 万次/月，邮箱注册）
LOCATIONIQ_KEY=

# ============================================================
# 可选上传限制，单位 MB
# ============================================================

# 单个地点素材，默认 250 MB
MAX_MEDIA_UPLOAD_MB=250

# 单个完整录像，默认 512 MB
MAX_RECORDING_UPLOAD_MB=512

# 单批 JPEG 帧，默认 64 MB
MAX_FRAME_BATCH_MB=64

# 行程 JSON，默认 10 MB
MAX_TRIPS_BODY_MB=10
