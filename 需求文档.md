# Travel Story 旅行规划与旅行纪录片生成平台

## 文档说明

这份文档对应当前开源版本。功能状态以仓库中的代码和可以运行的页面为准。

早期需求文档包含账号、AI 规划、自动读取 EXIF、音乐模板和云端协作等设想。这些内容没有从产品方向中删除，但不能继续与已经完成的功能写在一起。下文将功能分为已经实现、部分实现和尚未实现三类。

## 产品定位

Travel Story 是一个面向个人使用的旅行规划与旅行纪录片生成工具。

用户在旅行前创建行程，按天添加地点并选择交通方式。旅行结束后，用户可以为地点上传照片和视频。系统按照原有行程播放地图镜头、路线、地点字幕和素材，并生成一段旅行影片。

当前版本采用单用户自部署方式。部署者本人负责环境变量、本地数据、地图服务额度和备份。项目没有账号、权限和多租户隔离。

## 当前用户流程

### Plan

1. 在首页创建一段旅行
2. 设置名称、开始日期、结束日期、出发地、区域和说明
3. 按天维护行程
4. 搜索地点并加入时间线
5. 为相邻地点选择交通方式
6. 查看地图路线和镜头预览

### Record

1. 打开已有旅行
2. 为具体地点选择照片或视频
3. 浏览器将素材上传到部署机器
4. 素材元数据随行程保存，二进制文件保存在 `data/media/`

### Story

1. 检查行程至少包含两个地点
2. 选择横屏或竖屏
3. 选择 720p 或 1080p
4. 选择 30 FPS 或 60 FPS
5. 预热地图瓦片和字形
6. 播放路线、镜头、字幕和地点素材
7. 在浏览器录制或逐帧渲染
8. 由服务端保存或通过 FFmpeg 合成 MP4

## 已实现功能

### Plan 旅行规划

#### 旅行管理

- 创建旅行
- 编辑旅行基础信息
- 删除旅行
- 按开始日期生成第一天
- 添加和删除 Day
- 调整 Day 后重新编号
- 使用本地缓存提高首屏读取速度
- 将完整行程同步到服务端 `data/trips.json`

#### 地点管理

- 按 Day 添加地点
- 保存地点名称、城市、国家、经纬度和类型
- 删除地点
- 按 Day 与顺序归一化地点
- 自动重建相邻地点之间的 Trip Segment
- 删除地点后保留仍然有效的路线与交通信息

#### 地点搜索

地点搜索由服务端代理，密钥不会发到浏览器。

- 高德负责国内地点搜索
- LocationIQ 负责国外地点搜索和补充结果
- 两个来源并发查询
- 按名称匹配、地点类型和来源信息合并排序
- 对相近的同名地点去重
- 搜索结果写入服务端内存缓存
- 未配置密钥时向前端返回明确状态

#### 交通方式

当前数据模型支持以下交通方式。

```text
car
bus
train
subway
plane
ship
kayak
motorcycle
bicycle
scooter
walk
horse
ufo
swim
rocket
```

交通方式会被归入道路、铁路、空中、水上和步行类别。类别决定路线服务、载具图标和地图动画。

### Record 旅行记录

- 为地点上传图片
- 为地点上传视频
- 在地点时间线中预览素材
- 删除地点素材
- 将素材元数据保存在 TripStop
- 将素材二进制写入 `data/media/`
- 通过 Range 响应支持视频拖动播放
- 对素材 ID 做路径白名单检查
- 对 MIME 类型和上传体积做服务端检查

当前 Record 依赖用户手动把素材放到地点下面。系统不会根据拍摄时间和坐标自动分配素材。

### Story 旅行影片

#### 播放时间线

`lib/map/playback.ts` 根据地点和路段生成镜头序列。序列包括 Day 章节、地点镜头、路段镜头和片尾。

- 计算镜头开始时间与持续时间
- 在路线上按距离采样载具位置
- 控制已行驶路线高亮
- 切换 Mercator 与 Globe 投影
- 控制地图中心、缩放、方向和俯仰
- 播放结束或取消后恢复地图状态

#### 画布合成

`lib/record/compositor.ts` 将地图画布、字幕、地点图片和地点视频绘制到输出画布。

- 横屏 16:9
- 竖屏 9:16
- 720p
- 1080p
- 30 FPS
- 60 FPS
- Day 字幕
- 地点字幕
- 路段信息
- 图片展示
- 视频展示
- 片尾画面

#### 实时录制

`lib/record/realtime.ts` 使用 Canvas `captureStream()` 和 MediaRecorder 录制播放过程。

浏览器产出 WebM 时，服务端使用 FFmpeg 转成 H.264 MP4。转码失败时保留原始 WebM，避免丢失已经录好的内容。

#### 离线渲染

`lib/record/offline.ts` 支持两条输出路径。

- 浏览器支持 WebCodecs 时，编码 H.264 并使用 `mp4-muxer` 封装 MP4
- WebCodecs 不可用时，将画布编码为 JPEG 帧，分批上传后由 FFmpeg 合成 MP4

离线渲染会识别可以复用的静态帧，减少重复画布编码。渲染循环也会主动让出宏任务，避免页面长时间无法响应取消操作。

### 地图与路线

#### 底图

- 国内模式使用高德栅格底图
- 国际模式使用 OpenFreeMap 矢量底图
- 首页使用 MapLibre Globe 展示全球足迹
- 规划、播放和录制共用 TravelMapEngine
- 地图标记使用 HTML 覆盖层
- 地标和载具使用本地生成的 SVG

国际瓦片由 `/api/tiles` 代理。代理按以下顺序取数据。

1. `tile-cache/` 中的散片缓存
2. 可选的 `planet.mbtiles`
3. OpenFreeMap 上游

TileJSON 会改写瓦片地址，使浏览器继续经过本地代理。散片写入使用临时文件和 rename，避免半个缓存文件被长期使用。

#### 路线

- 道路路线使用 OSRM 公共演示服务
- 国内步行和骑行路线优先使用高德路径规划
- 飞机使用球面大圆线
- 轮船、皮划艇和游泳使用水上直连线
- 外部路线失败时回退为直连线
- 降级直线不会作为权威道路路线持久化

高德坐标在服务端和客户端边界转换为 WGS-84。国内底图显示时再转为 GCJ-02。

### 本地存储

当前存储分为三层。

#### 浏览器缓存

`lib/store.ts` 使用 localStorage 保存行程快照。它用于首屏读取和服务端不可用时的降级。

#### 服务端行程库

`lib/server/db.ts` 将完整行程写入 `data/trips.json`。写入使用临时文件和 rename。

#### 二进制文件

- 地点素材保存在 `data/media/`
- 生成的影片保存在 `data/recordings/`
- JPEG 临时帧保存在 `data/recordings/frames-<session>/`
- 国际地图瓦片保存在 `tile-cache/`

`data/`、`tile-cache/` 和本地环境变量都被 Git 忽略。

## 部分实现

### 足迹元数据

地点搜索会保存城市和国家。逆地理编码接口也可以补充城市和国家，但旧行程和手动构造的数据不保证包含完整字段。

### 地图预热

播放和录制前会沿时间线预热地图瓦片。它能减少录制中的空白瓦片，但结果仍受网络、浏览器缓存和上游服务影响。

### WebCodecs

离线渲染会检测浏览器的 H.264 WebCodecs 支持。不同浏览器、操作系统和显卡驱动的结果不同，因此 WebCodecs 不能作为唯一输出路径。

### JPEG 帧回退

不支持 WebCodecs 时可以上传 JPEG 帧并交给 FFmpeg 合成。长影片会产生大量临时文件，占用磁盘并增加编码时间。

### FFmpeg 与 NVENC

FFmpeg 需要由部署者安装。服务端会先尝试 NVIDIA NVENC，失败后使用 libx264。FFmpeg 的编译选项、驱动和显卡型号会影响 NVENC 是否可用。

### 公共地图服务

OpenFreeMap、OSRM、高德和 LocationIQ 都属于外部服务。当前实现包含超时、缓存和降级，但不承诺上游永久可用。

## 尚未实现

以下内容没有进入当前源码。

- 用户注册与登录
- 多用户数据隔离
- 公开分享页面
- 权限与访问控制
- PostgreSQL 或其他数据库
- S3 或其他对象存储
- 多设备同步
- 协作编辑
- AI 自动规划行程
- AI 生成旅行文案和标题
- 根据 EXIF 自动重建旅行路线
- 自动识别照片内容
- 背景音乐库
- 多套影片主题模板
- 在线任务队列
- 服务端分布式渲染
- 手机原生应用

Trip 中仍保留 `isPublic` 字段，但当前版本没有公开分享接口。这个字段属于未来兼容位，不能理解为公开功能已经完成。

## 当前数据模型

### Trip

```ts
interface Trip {
  id: string;
  name: string;
  startDate: string;
  endDate: string;
  origin?: string;
  region?: string;
  description?: string;
  isPublic: boolean;
  coverHue?: number;
  createdAt: number;
  days: TripDay[];
  stops: TripStop[];
  segments: TripSegment[];
}
```

### TripDay

```ts
interface TripDay {
  id: string;
  day: number;
  date: string;
}
```

### TripStop

```ts
interface TripStop {
  id: string;
  dayId: string;
  day: number;
  name: string;
  city?: string;
  country?: string;
  latitude: number;
  longitude: number;
  type: StopType;
  order: number;
  media?: MediaMeta[];
}
```

### TripSegment

```ts
interface TripSegment {
  id: string;
  fromStopId: string;
  toStopId: string;
  transport: Transport;
  route: RouteGeometry | null;
  distance?: number;
  duration?: number;
}
```

### MediaMeta

```ts
interface MediaMeta {
  id: string;
  kind: "image" | "video";
  name: string;
  createdAt: number;
}
```

## 当前技术架构

### 页面

| 路径 | 职责 |
| --- | --- |
| `app/page.tsx` | 首页、旅行列表、全球足迹 |
| `app/trip/[id]/page.tsx` | 行程编辑和素材管理 |
| `app/trip/[id]/play/page.tsx` | 地图故事预览 |
| `app/trip/[id]/record/page.tsx` | 影片规格选择、录制和导出 |

### 数据与领域逻辑

| 模块 | 职责 |
| --- | --- |
| `lib/types.ts` | Trip、Day、Stop、Segment 和素材类型 |
| `lib/store.ts` | 行程操作、归一化、本地缓存和服务端同步 |
| `lib/useStore.ts` | React 外部存储订阅 |
| `lib/server/db.ts` | 服务端平面文件读写 |
| `lib/media.ts` | 素材上传、删除和访问地址 |

### 地图

| 模块 | 职责 |
| --- | --- |
| `lib/map/engine.ts` | MapLibre 实例、图层、路线和载具 |
| `lib/map/style.ts` | 国内与国际底图样式 |
| `lib/map/playback.ts` | 镜头时间线和播放控制 |
| `lib/map/prefetch.ts` | 瓦片预热 |
| `lib/map/captions.ts` | 播放和录制字幕 |
| `lib/routing.ts` | 路线服务与几何采样 |

### 录制

| 模块 | 职责 |
| --- | --- |
| `lib/record/compositor.ts` | 输出画布和素材合成 |
| `lib/record/realtime.ts` | MediaRecorder 实时录制 |
| `lib/record/offline.ts` | WebCodecs 与 JPEG 帧离线渲染 |
| `lib/record/prewarm.ts` | 播放前地图预热 |

## 降级与错误处理

- 地图初始化失败时，规划页面保留编辑能力并显示重试入口
- 未配置地点密钥时，搜索返回明确的未配置状态
- 单个地点服务失败时，另一个来源仍可返回结果
- 道路服务失败时，地图回退直连线
- WebCodecs 不可用时，回退 JPEG 帧
- NVENC 不可用时，回退 libx264
- WebM 转 MP4 失败时，保留原始 WebM
- 服务端同步失败时，浏览器继续使用 localStorage
- 媒体元数据丢失时，二进制文件仍可用默认类型读取

## 开源版本边界

当前版本适合项目所有者本人使用，或者运行在可信局域网中。

业务 API 没有鉴权。能够访问服务的人可以读写行程、上传或删除素材、提交录像和触发 FFmpeg。上传接口有体积和格式检查，这些检查用于避免误操作与明显的资源消耗，不能替代身份验证。

如果要把服务直接暴露给陌生用户，需要新增以下能力。

- 身份验证
- 每个用户独立的数据目录或数据库记录
- 对象存储权限
- 跨实例速率限制
- HTTPS
- 上传内容检查
- 后台任务队列
- 磁盘配额
- 审计日志
- 备份与恢复流程

## 后续开发顺序

### 第一阶段

- 补齐 README 应用截图
- 增加核心数据归一化测试
- 增加 API 路由集成测试
- 为生成影片补充可重复的验收样例
- 处理 Next.js、PostCSS 和 sharp 的已知安全更新
- 评估 `mp4-muxer` 的替代方案

### 第二阶段

- 将行程和素材迁移到数据库与对象存储
- 增加单用户登录保护
- 增加影片生成任务状态
- 增加失败重试与磁盘清理策略
- 拆分首页地图代码，减少初次加载体积

### 第三阶段

- 多用户隔离
- 公开分享
- 多设备同步
- 自动读取 EXIF
- 影片模板与音乐
- AI 辅助规划与文案
